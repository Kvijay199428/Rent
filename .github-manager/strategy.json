{
  "strategy": "alpha-beta-release (dev-production-main)",
  "reason": [
    "Explicit owner request: three-tier branch flow — 'dev' is the developer branch (alpha, initial build for development), 'production' is the production-grade branch (beta, candidate for release after bug fixes), 'main' is the release branch",
    "deploy.py / self-pull.sh are branch-aware: --dev deploys 'dev' (alpha), --prod deploys 'production' (beta), --release deploys 'main' (release)",
    "All three branches have distinct roles; 'main' is retained as the release branch and is NOT deleted",
    "All three branches were initialized from the same creation point (df6264b) and follow a linear alpha -> beta -> release flow"
  ],
  "branches": {
    "development": "dev",
    "production": "production",
    "release": "main"
  },
  "feature_branches": { "enabled": true, "pattern": "feature/*" },
  "hotfix_branches": { "enabled": true, "pattern": "hotfix/*" },
  "release_branches": { "enabled": true, "branch": "main" }
}