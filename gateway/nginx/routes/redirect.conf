# Frontend routes are NOT served on the API host. The public frontend lives on
# rent.vijaykrsha.online (Cloudflare Pages), which serves tenant portal pages
# directly under the canonical /tenant/{landlordUuid}/QR/... scheme via the
# Pages _redirects SPA fallback. The legacy root-level tenant portal deep-link
# redirect (/<landlordUuid>/t/<propertyId>/<tenantId>/<viewToken>) was removed
# together with the old URL scheme; tenant API paths under /<landlordUuid>/t/...
# /api/ are handled by the ^~ locations in api.conf before this file applies.